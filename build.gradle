plugins {
    id 'java'
}

group 'bisq'

repositories {
    mavenCentral()
}

apply from: 'buildSrc/bisq-version.gradle'
apply from: 'buildSrc/os-arch.gradle'

dependencies {
}

test {
    useJUnitPlatform()
    exclude '**/**Integration*'
}