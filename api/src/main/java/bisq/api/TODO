Open tasks:

- test all scenarios with server variants and api modes if authn and authz is done
- separate transport and api level filtering
- add permissions for WS topics
- Maybe remove deny/allow list as redundant with permissions
- add button for detect lan address
- handle persistence maps with old entries
- SessionToken TTL -> config