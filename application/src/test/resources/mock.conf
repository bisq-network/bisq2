bisq {
    network = {
        networkIOPool {
            name = "NETWORK_IO_POOL"
            corePoolSize = 1
            maximumPoolSize = 5000
            keepAliveTimeInSec = 10
        }
    }
}
